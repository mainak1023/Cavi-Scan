
import React from 'react';
import { Sun } from 'lucide-react';
import { Button } from '@/components/ui/button';

export function ThemeToggle() {
  // Empty component that doesn't do anything since we're only using light mode
  return null;
}
